import { createFileRoute } from '@tanstack/react-router'
import { Building2, LockKeyhole, ShieldCheck, SlidersHorizontal } from 'lucide-react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export const Route = createFileRoute('/app/settings')({ head: () => ({ meta: [{ title: 'Settings · Sentinel ONE' }] }), component: SettingsPage })
function SettingsPage() { return <div className="space-y-6 p-5 lg:p-8"><div><p className="font-mono text-[11px] uppercase tracking-[0.18em] text-primary">Administration / Configuration</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Settings</h1><p className="mt-1 text-sm text-muted-foreground">Configure the organization, access model, and integrations.</p></div><div className="grid gap-5 md:grid-cols-2">{[[Building2, 'Organization', 'Municipality identity, contact details, and departments.'], [ShieldCheck, 'Roles & permissions', 'Data-driven access policies for every platform role.'], [LockKeyhole, 'Security', 'Sessions, verification, audit retention, and API access.'], [SlidersHorizontal, 'Integrations', 'Map, storage, notifications, and AI provider adapters.']].map(([Icon, title, description]) => <Card key={title as string} className="transition hover:-translate-y-0.5 hover:shadow-md"><CardHeader><div className="grid size-10 place-items-center rounded-lg bg-secondary text-primary"><Icon className="size-5" /></div><CardTitle className="mt-2 text-lg">{title as string}</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-muted-foreground">{description as string}</p></CardContent></Card>)}</div></div>}
