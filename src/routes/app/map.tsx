import { createFileRoute } from '@tanstack/react-router'
import { Layers3, MapPin, Navigation, Plus, ShieldCheck, ZoomIn } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export const Route = createFileRoute('/app/map')({ head: () => ({ meta: [{ title: 'Operations map · Sentinel ONE' }] }), component: MapPage })
function MapPage() { return <div className="space-y-6 p-5 lg:p-8"><div><p className="font-mono text-[11px] uppercase tracking-[0.18em] text-primary">Operations / Situational view</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Operations map</h1><p className="mt-1 text-sm text-muted-foreground">A provider-neutral map surface for authorized incidents, crews, and assets.</p></div><Card className="overflow-hidden"><CardHeader className="flex flex-row items-center justify-between border-b border-border"><CardTitle className="flex items-center gap-2 text-lg"><Layers3 className="size-5 text-primary" /> Map layers</CardTitle><div className="flex gap-2"><Button variant="outline" size="sm"><Navigation className="size-4" /> Workers</Button><Button variant="outline" size="sm"><ShieldCheck className="size-4" /> Incidents</Button></div></CardHeader><CardContent className="p-0"><div className="relative min-h-[520px] overflow-hidden bg-secondary/60"><div className="absolute inset-0 opacity-40" style={{ backgroundImage: 'linear-gradient(to right, oklch(0.5 0.05 190 / 0.16) 1px, transparent 1px), linear-gradient(to bottom, oklch(0.5 0.05 190 / 0.16) 1px, transparent 1px)', backgroundSize: '48px 48px' }} /><div className="absolute left-[28%] top-[30%] grid size-11 place-items-center rounded-full border-4 border-destructive/25 bg-destructive text-destructive-foreground shadow-lg"><MapPin className="size-5" /></div><div className="absolute right-[24%] top-[47%] grid size-11 place-items-center rounded-full border-4 border-primary/25 bg-primary text-primary-foreground shadow-lg"><MapPin className="size-5" /></div><div className="absolute left-[48%] bottom-[22%] grid size-11 place-items-center rounded-full border-4 border-accent/50 bg-accent text-accent-foreground shadow-lg"><MapPin className="size-5" /></div><div className="absolute right-4 top-4 flex flex-col gap-2"><Button variant="outline" size="icon" className="bg-card"><Plus className="size-4" /></Button><Button variant="outline" size="icon" className="bg-card"><ZoomIn className="size-4" /></Button></div><div className="absolute bottom-5 left-5 max-w-sm rounded-xl border border-border bg-card/95 p-4 shadow-lg backdrop-blur"><p className="text-sm font-semibold">Provider-neutral map surface</p><p className="mt-1 text-xs leading-5 text-muted-foreground">No GPS or live location is fabricated. Connect your approved municipal map provider to activate geospatial data.</p><div className="mt-3 flex items-center gap-2 text-xs text-primary"><MapPin className="size-3.5" /> 3 sample incident focus points</div></div></div></CardContent></Card></div> }
