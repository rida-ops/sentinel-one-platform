import { createFileRoute } from '@tanstack/react-router'
import { CheckCircle2, ClipboardList, Clock3, MapPin } from 'lucide-react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export const Route = createFileRoute('/app/tasks')({ head: () => ({ meta: [{ title: 'Tasks · Sentinel ONE' }] }), component: TasksPage })
function TasksPage() { return <div className="space-y-6 p-5 lg:p-8"><div><p className="font-mono text-[11px] uppercase tracking-[0.18em] text-primary">Operations / Work orders</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Tasks</h1><p className="mt-1 text-sm text-muted-foreground">Track field work from assignment through verified completion.</p></div><div className="grid gap-4 sm:grid-cols-3"><Card><CardContent className="flex items-center gap-3 p-5"><Clock3 className="size-5 text-accent-foreground" /><div><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-semibold">08</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><ClipboardList className="size-5 text-primary" /><div><p className="text-xs text-muted-foreground">In progress</p><p className="text-2xl font-semibold">12</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><CheckCircle2 className="size-5 text-chart-2" /><div><p className="text-xs text-muted-foreground">Completed today</p><p className="text-2xl font-semibold">17</p></div></CardContent></Card></div><Card><CardHeader className="border-b border-border"><CardTitle className="text-lg">Active work orders</CardTitle></CardHeader><CardContent className="space-y-3 pt-5">{['Inspect valve chamber at Civic Plaza', 'Secure East Market perimeter', 'Replace signal controller at 5th & Pine'].map((task, index) => <div key={task} className="flex items-center gap-4 rounded-lg border border-border p-4"><div className="grid size-9 place-items-center rounded-lg bg-secondary text-primary"><ClipboardList className="size-4" /></div><div className="flex-1"><p className="text-sm font-medium">{task}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin className="size-3" /> Sector {index + 2} · {index === 1 ? 'Critical' : 'High'} priority</p></div><span className="rounded-full bg-accent/30 px-2 py-1 text-[10px] font-semibold text-accent-foreground">{index === 0 ? 'Assigned' : 'In progress'}</span></div>)}</CardContent></Card></div>}
