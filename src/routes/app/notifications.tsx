import { createFileRoute } from '@tanstack/react-router'
import { Bell, CheckCircle2 } from 'lucide-react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export const Route = createFileRoute('/app/notifications')({ head: () => ({ meta: [{ title: 'Notifications · Sentinel ONE' }] }), component: NotificationsPage })
function NotificationsPage() { return <div className="space-y-6 p-5 lg:p-8"><div><p className="font-mono text-[11px] uppercase tracking-[0.18em] text-primary">Operations / Activity</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Notifications</h1><p className="mt-1 text-sm text-muted-foreground">Important updates from the unified response network.</p></div><Card><CardHeader className="border-b border-border"><CardTitle className="flex items-center gap-2 text-lg"><Bell className="size-5 text-primary" /> Recent activity</CardTitle></CardHeader><CardContent className="space-y-4 pt-5">{['Critical incident acknowledged by Fire dispatch', 'Water team availability changed to available', 'New report received from Civic Plaza'].map((text, i) => <div key={text} className="flex gap-3"><div className="mt-1 grid size-8 place-items-center rounded-full bg-secondary text-primary"><Bell className="size-3.5" /></div><div><p className="text-sm font-medium">{text}</p><p className="mt-1 text-xs text-muted-foreground">{i + 1} hour{i ? 's' : ''} ago · Sentinel ONE</p></div></div>)}</CardContent></Card><div className="flex items-center gap-3 rounded-lg border border-dashed border-border bg-muted/30 p-4 text-sm text-muted-foreground"><CheckCircle2 className="size-4 text-chart-2" /> Notification delivery channels are ready for in-app, email, push, and SMS adapters.</div></div>}
