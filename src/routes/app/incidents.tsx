import { createFileRoute } from '@tanstack/react-router'
import { AlertTriangle, ChevronDown, Filter, Search, Waves } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'

const rows = [
  ['S1-26-00481', 'Water main break near Civic Plaza', 'Water', 'High', 'Unassigned', '04 min'],
  ['S1-26-00479', 'Smoke reported at East Market', 'Fire', 'Critical', 'Dispatched', '09 min'],
  ['S1-26-00474', 'Signal outage at 5th & Pine', 'Electricity', 'Medium', 'In progress', '18 min'],
  ['S1-26-00466', 'Pothole creating lane hazard', 'Roads', 'Medium', 'Under review', '31 min'],
]

export const Route = createFileRoute('/app/incidents')({ head: () => ({ meta: [{ title: 'Incident queue · Sentinel ONE' }] }), component: IncidentsPage })

function IncidentsPage() { return <div className="space-y-6 p-5 lg:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="font-mono text-[11px] uppercase tracking-[0.18em] text-primary">Operations / Incidents</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Incident queue</h1><p className="mt-1 text-sm text-muted-foreground">Review, verify, and route incoming public-service reports.</p></div><Button><Filter className="size-4" /> Filters</Button></div><div className="grid gap-4 sm:grid-cols-3"><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Unassigned</p><p className="mt-1 text-2xl font-semibold">06</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Under review</p><p className="mt-1 text-2xl font-semibold">11</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Critical</p><p className="mt-1 text-2xl font-semibold text-destructive">02</p></CardContent></Card></div><Card><CardHeader className="flex flex-col gap-4 border-b border-border pb-4 sm:flex-row sm:items-center sm:justify-between"><CardTitle className="text-lg">All active reports</CardTitle><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><Input className="pl-9" placeholder="Search reference or title" /></div></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-muted/50 text-xs text-muted-foreground"><tr>{['Reference', 'Incident', 'Department', 'Priority', 'Status', 'Age', ''].map(head => <th key={head} className="px-5 py-3 font-medium">{head}</th>)}</tr></thead><tbody>{rows.map(([ref, title, department, priority, status, age]) => <tr key={ref} className="border-t border-border transition hover:bg-muted/30"><td className="px-5 py-4 font-mono text-xs text-muted-foreground">{ref}</td><td className="px-5 py-4 font-medium"><div className="flex items-center gap-2"><Waves className="size-4 text-primary" />{title}</div></td><td className="px-5 py-4 text-muted-foreground">{department}</td><td className="px-5 py-4"><span className={`rounded-full px-2 py-1 text-[10px] font-semibold ${priority === 'Critical' ? 'bg-destructive/10 text-destructive' : 'bg-accent/35 text-accent-foreground'}`}>{priority}</span></td><td className="px-5 py-4 text-muted-foreground">{status}</td><td className="px-5 py-4 font-mono text-xs text-muted-foreground">{age}</td><td className="px-5 py-4"><Button variant="ghost" size="sm">Open <ChevronDown className="size-3.5 -rotate-90" /></Button></td></tr>)}</tbody></table></div></CardContent></Card><div className="flex items-center gap-3 rounded-lg border border-dashed border-border bg-muted/30 p-4 text-sm text-muted-foreground"><AlertTriangle className="size-4 text-accent-foreground" /> Queue data is ready for connection to authenticated incident records and live dispatch events.</div></div> }
